import { StyleSheet } from 'react-native';
import { type ColorScheme, Typography, Spacing, Radius } from '../../theme';

export const makeStyles = (c: ColorScheme) => StyleSheet.create({
  safe: { flex: 1, backgroundColor: c.background },
  container: { padding: Spacing.md, paddingBottom: Spacing.xxl },
  sectionLabel: {
    fontSize: Typography.size.xs,
    fontWeight: Typography.weight.bold,
    color: c.textMuted,
    letterSpacing: 1.5,
    marginVertical: Spacing.sm,
  },
  card: {
    backgroundColor: c.surface,
    borderRadius: Radius.lg,
    padding: Spacing.md,
    marginBottom: Spacing.md,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 1 },
    shadowOpacity: 0.05,
    shadowRadius: 3,
    elevation: 1,
  },
  timeHeader: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: Spacing.md,
    marginBottom: 4,
  },
  timeLabel: { fontSize: Typography.size.sm, color: c.textMuted },
  bigTime: {
    fontSize: Typography.size.xxl,
    fontWeight: Typography.weight.bold,
    color: c.primary,
    marginBottom: Spacing.md,
  },
  timeChips: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: Spacing.sm,
    marginBottom: Spacing.md,
  },
  chip: {
    borderWidth: 1,
    borderColor: c.border,
    borderRadius: Radius.full,
    paddingHorizontal: Spacing.md,
    paddingVertical: Spacing.xs,
  },
  chipActive: { backgroundColor: c.primary, borderColor: c.primary },
  chipText: { fontSize: Typography.size.sm, color: c.textSecondary },
  chipTextActive: { color: c.textOnPrimary, fontWeight: Typography.weight.semiBold },
  repeatLabel: {
    fontSize: Typography.size.xs,
    fontWeight: Typography.weight.bold,
    color: c.textMuted,
    letterSpacing: 1.2,
    marginBottom: Spacing.sm,
  },
  daysRow: { flexDirection: 'row', justifyContent: 'space-between' },
  dayCircle: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: c.surface,
    borderWidth: 1,
    borderColor: c.border,
    alignItems: 'center',
    justifyContent: 'center',
  },
  dayCircleActive: {
    backgroundColor: c.dayCircleActive,
    borderColor: c.dayCircleActive,
  },
  dayText: { fontSize: Typography.size.sm, fontWeight: Typography.weight.medium, color: c.textSecondary },
  dayTextActive: { color: c.textOnPrimary, fontWeight: Typography.weight.bold },
  previewCard: {
    backgroundColor: c.primaryDark,
    borderRadius: Radius.lg,
    padding: Spacing.md,
    marginBottom: Spacing.md,
  },
  previewInner: {
    backgroundColor: c.surface,
    borderRadius: Radius.md,
    padding: Spacing.md,
    flexDirection: 'row',
    alignItems: 'flex-start',
    marginBottom: Spacing.sm,
  },
  previewIcon: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: c.primary,
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: Spacing.sm,
  },
  previewContent: { flex: 1 },
  previewApp: {
    fontSize: Typography.size.xs,
    fontWeight: Typography.weight.bold,
    color: c.textMuted,
    letterSpacing: 1,
    marginBottom: 2,
  },
  previewTitle: {
    fontSize: Typography.size.sm,
    fontWeight: Typography.weight.bold,
    color: c.textPrimary,
    marginBottom: 2,
  },
  previewBody: { fontSize: Typography.size.xs, color: c.textSecondary },
  previewTime: { fontSize: Typography.size.xs, color: c.textMuted },
  previewMode: {
    fontSize: Typography.size.xs,
    color: 'rgba(255,255,255,0.6)',
    textAlign: 'right',
  },
  prefCard: {
    backgroundColor: c.surface,
    borderRadius: Radius.lg,
    paddingHorizontal: Spacing.md,
    marginBottom: Spacing.md,
  },
  lastRow: { borderBottomWidth: 0 },
  footer: {
    fontSize: Typography.size.xs,
    color: c.textMuted,
    textAlign: 'center',
    fontStyle: 'italic',
    lineHeight: Typography.size.xs * 1.8,
    paddingHorizontal: Spacing.md,
    marginTop: Spacing.md,
  },
}
);