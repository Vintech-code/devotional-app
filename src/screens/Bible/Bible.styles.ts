import { StyleSheet } from 'react-native';
import { type ColorScheme, Typography, Spacing, Radius } from '../../theme';

export const makeStyles = (c: ColorScheme) => StyleSheet.create({
  safe: { flex: 1, backgroundColor: c.background },

  header: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    paddingHorizontal: Spacing.md,
    paddingVertical: Spacing.md,
    backgroundColor: c.surface,
    borderBottomWidth: 1,
    borderBottomColor: c.border,
  },
  headerTitle: {
    fontSize: Typography.size.lg,
    fontWeight: Typography.weight.bold,
    color: c.textPrimary,
  },
  searchBtn: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: c.surfaceAlt,
    alignItems: 'center',
    justifyContent: 'center',
  },

  searchBar: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: c.surface,
    paddingHorizontal: Spacing.md,
    paddingVertical: Spacing.sm,
    borderBottomWidth: 1,
    borderBottomColor: c.border,
  },
  searchInput: { flex: 1, fontSize: Typography.size.md, color: c.textPrimary },

  navBar: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: Spacing.md,
    paddingVertical: Spacing.sm,
    backgroundColor: c.surface,
    borderBottomWidth: 1,
    borderBottomColor: c.border,
  },
  selector: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: c.surfaceAlt,
    borderRadius: Radius.sm,
    paddingHorizontal: Spacing.sm,
    paddingVertical: 6,
    marginRight: 4,
  },
  selectorBook: {
    fontSize: Typography.size.md,
    fontWeight: Typography.weight.semiBold,
    color: c.primary,
    marginRight: 4,
  },
  selectorChapter: {
    fontSize: Typography.size.md,
    fontWeight: Typography.weight.semiBold,
    color: c.primary,
    marginRight: 4,
  },
  navButtons: { flexDirection: 'row', marginLeft: 'auto', gap: 4 },
  navBtn: {
    width: 32,
    height: 32,
    borderRadius: 16,
    backgroundColor: c.surfaceAlt,
    alignItems: 'center',
    justifyContent: 'center',
  },
  dropdown: {
    backgroundColor: c.surface,
    borderBottomWidth: 1,
    borderBottomColor: c.border,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 4,
    zIndex: 100,
  },
  dropItem: {
    paddingHorizontal: Spacing.md,
    paddingVertical: 12,
    borderBottomWidth: 1,
    borderBottomColor: c.border,
  },
  dropItemActive: { backgroundColor: c.surfaceAlt },
  dropText: { fontSize: Typography.size.md, color: c.textPrimary },
  dropTextActive: { color: c.primary, fontWeight: Typography.weight.semiBold },
  chapterGrid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    padding: Spacing.sm,
    gap: 6,
  },
  chapterDot: {
    width: 44,
    height: 44,
    borderRadius: 22,
    backgroundColor: c.surfaceAlt,
    alignItems: 'center',
    justifyContent: 'center',
  },
  chapterDotActive: { backgroundColor: c.primary },
  chapterDotText: { fontSize: Typography.size.md, color: c.textPrimary },
  chapterDotTextActive: { color: c.textOnPrimary, fontWeight: Typography.weight.bold },

  verseList: { flex: 1 },
  verseListContent: { padding: Spacing.md, paddingBottom: Spacing.xxl },
  chapterHeading: {
    fontSize: Typography.size.display,
    fontFamily: Typography.fontFamilySerif,
    fontWeight: Typography.weight.bold,
    color: c.primary,
    marginBottom: Spacing.md,
  },
  verseRow: {
    flexDirection: 'row',
    paddingVertical: Spacing.sm,
    paddingHorizontal: Spacing.xs,
    borderRadius: Radius.sm,
    marginBottom: 2,
  },
  verseRowSelected: { backgroundColor: c.tagActive },
  verseNum: {
    width: 28,
    fontSize: Typography.size.sm,
    fontWeight: Typography.weight.bold,
    color: c.accent,
    paddingTop: 2,
  },
  verseText: {
    flex: 1,
    fontSize: Typography.size.md,
    fontFamily: Typography.fontFamilySerif,
    color: c.textPrimary,
    lineHeight: 28,
  },
  verseTextSelected: { color: c.primary },

  hintRow: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: c.surfaceAlt,
    borderRadius: Radius.md,
    padding: Spacing.md,
    marginTop: Spacing.lg,
  },
  hintText: { flex: 1, fontSize: Typography.size.sm, color: c.textSecondary },

  actionBar: {
    flexDirection: 'row',
    backgroundColor: c.primary,
    paddingVertical: Spacing.sm,
  },
  actionBtn: { flex: 1, alignItems: 'center', paddingVertical: Spacing.xs },
  actionText: {
    fontSize: 9,
    color: c.textOnPrimary,
    fontWeight: Typography.weight.bold,
    letterSpacing: 0.5,
  },
});